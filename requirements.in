# Dependências de runtime (alto nível). Versões fixas e hashes são geradas em
# requirements.txt via pip-compile. Mantenha esta lista mínima: cada pacote é
# superfície de ataque de supply chain.
numpy>=2.3.3
scikit-learn>=1.3
pandas>=3.0.3
pip-tools